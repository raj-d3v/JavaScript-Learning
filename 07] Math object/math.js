// Math = built-in object that provides a
//        collection of properties and methods

// Math method is used with the 'M' capital

let v = -7;
let w = 3.21;
let x = 3;
let y = 2;
let z;

/* Rounds a number */
// z = Math.round(w);

/* Rounds a number down */
// z = Math.floor(w);

/* Rounds a number up */
// z = Math.ceil(w);

/* To use power */
// z = Math.pow(x,y);

/* Square root */
// z = Math.sqrt(81);

/* Absolute value i.e negative to positive */
// z = Math.abs(v);

// console.log(z);

/* Random float between 0 and 1 */
// let randomFloat = Math.random();
// console.log("Random float between 0 and 1:", randomFloat);

/* Random float between 1 and 30 */
// let randomFloatRange = Math.random() * (30 - 1) + 1;
// console.log("Random float between 1 and 30:", randomFloatRange);

/* Random integer between 1 and 30 */
// let randomNumber = Math.floor(Math.random() * 30) + 1;
// console.log("Random integer between 1 and 30:", randomNumber);

/* Max and Min of a number */

let a = 6;
let b = 9;

let max = Math.max(a, b);
console.log(max);

let min = Math.min(a, b);
console.log(min);
